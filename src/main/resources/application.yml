spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/user-balance
    username: solidgate
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
app:
  batchSize: 1000